# حالت پایلوت یک‌هفته‌ای (Pilot Mode)

این سند وضعیت پورتال را در **دورهٔ پایلوت یک‌هفته‌ای با دادهٔ واقعی و بدون اتصال ERP** توضیح می‌دهد.

اصل حاکم بر این فاز: **هیچ کدی حذف یا بازنویسی نشده است.** تمام تفاوت‌های رفتاری پشت
Feature Flag هستند؛ با برعکس کردن مقدار پرچم‌ها در `.env` و ری‌استارت API، رفتار قبلی
عیناً و بدون تغییر کد برمی‌گردد.

---

## ۱. فهرست کامل Feature Flagها

| پرچم (کلید `.env`) | مقدار در پایلوت | مقدار پس از پایلوت | اثر وقتی روشن است |
|---|---|---|---|
| `FEATURE_FINANCE_ENABLED` | `false` | `true` | بخش «مالی» در پورتال مشتری فعال است و `/finance/*` پاسخ عادی می‌دهد |
| `FEATURE_ORDERS_ENABLED` | `false` | `true` | بخش «سفارشات» فعال است و `/orders/*` پاسخ عادی می‌دهد |
| `FEATURE_MANUAL_ORDER_ENTRY` | `true` | `false` | دکمهٔ «ثبت سفارش دستی (پایلوت)» و مسیرهای `/admin/manual-orders/*` |
| `FEATURE_MANUAL_DELIVERY_ENTRY` | `true` | `false` | دکمهٔ «ثبت تحویل (پایلوت)» و مسیرهای `/admin/loading-requests/:id/register-delivery` و `/admin/loading-requests/carriers` |

پیش‌فرض‌های کد (وقتی کلید در `.env` نباشد) در
`packages/shared-types/src/feature-flags.ts` → `FEATURE_FLAG_DEFAULTS` تعریف شده و
**برابر حالت پس از پایلوت** است: مالی/سفارشات روشن، ثبت دستی خاموش. یعنی اگر روزی
`.env` را از دست بدهیم، سیستم به حالت عادی برمی‌گردد نه حالت پایلوت.

قرارداد خواندن مقدار: فقط رشتهٔ `true` (بدون حساسیت به بزرگی/کوچکی) یعنی روشن.
هر مقدار دیگر یعنی خاموش. (`apps/api/src/common/services/feature-flags.service.ts`)

### پرچم‌ها را کجا ست کنیم؟ (دو محیط اجرا، دو محل)

| نحوهٔ اجرای API | فایل معتبر | دلیل |
|---|---|---|
| محلی (`pnpm dev` / `pnpm test`) | `apps/api/.env` | Prisma CLI و `ConfigModule` فایل `.env` را نسبت به cwd می‌خوانند و cwd همان `apps/api` است |
| داخل Docker (`docker compose up backend`) | `.env` ریشه → `docker-compose.yml` | کانتینر `apps/api/.env` را نمی‌بیند؛ آن فایل در `.gitignore` است و به Image کپی نمی‌شود |

⚠️ در `docker-compose.yml` سرویس `backend` هر چهار پرچم را با پیش‌فرضِ **مقادیر
پایلوت** می‌گیرد. اگر این چهار خط حذف شوند، کد به `FEATURE_FLAG_DEFAULTS` برمی‌گردد
که «حالت پس از پایلوت» است و پایلوت بی‌صدا خاموش می‌شود.

### نحوهٔ رسیدن پرچم‌ها به Frontend

Frontend مقادیر را در زمان اجرا از `GET /api/v1/feature-flags` می‌گیرد
(`apps/web/lib/feature-flags.tsx`)، نه از متغیرهای `NEXT_PUBLIC_`. بنابراین برای
تغییر هر پرچم فقط **ری‌استارت API** لازم است و نیازی به Build مجدد Frontend نیست.

---

## ۲. رفتار دو بخش غیرفعال (مالی و سفارشات)

- **Navbar:** آیتم‌ها حذف یا مخفی نمی‌شوند؛ کم‌رنگ (`opacity-50 cursor-not-allowed`)
  با برچسب «مالی (غیرفعال)» و «سفارشات (غیرفعال)» نمایش داده می‌شوند. عمداً به‌جای
  `<Link>` یک `<button disabled>` رندر می‌شود تا کلیک هیچ Navigation‌ای انجام ندهد و
  Tooltip «این بخش به‌زودی در دسترس قرار می‌گیرد» دیده شود.
- **Backend:** `FeatureFlagGuard` روی کنترلرهای `orders` و `finance` خطای
  `FEATURE_DISABLED` را با **HTTP 403** برمی‌گرداند (نه 404) — مطابق کاتالوگ خطای
  بخش ۱۶ PRD:
  ```json
  { "code": "FEATURE_DISABLED", "message": "این بخش موقتاً غیرفعال است" }
  ```
- کدِ خودِ این دو ماژول **دست نخورده** است؛ با `true` شدن پرچم‌ها همان کد تست‌شدهٔ
  فعلی بدون تغییر اجرا می‌شود.

---

## ۳. ثبت دستی سفارش و تحویل

### سفارش دستی
- `Order.source` از نوع `OrderSource` با مقادیر `MANUAL` و `ERP_SYNC`
  (پیش‌فرض `ERP_SYNC`، ایندکس‌دار). سفارش‌های ثبت‌شده در پایلوت `MANUAL` هستند و
  شمارهٔ آن‌ها با پیشوند `MO-` تولید می‌شود تا در زمان بایگانی به‌سادگی قابل تشخیص باشند.
- ادمین می‌تواند مقدار سفارش دستی را بعداً **افزایش/اصلاح** کند؛ مقدار جدید هرگز
  نمی‌تواند کمتر از مقدار تحویل‌شده تا آن لحظه باشد. هر ایجاد/ویرایش در
  `AuditLog` با اکشن `MANUAL_ORDER_CREATE` / `MANUAL_ORDER_UPDATE_QTY` ثبت می‌شود.
- تمام Queryهای مخزن سفارش دستی به `source = MANUAL` محدودند، پس سفارش‌های ERP از
  این مسیر قابل تغییر نیستند (دفاع در عمق، مستقل از پرچم).
- فرم «اعلام بار» مشتری هیچ تغییری نکرده است.

### تحویل دستی
- «ثبت تحویل (پایلوت)» فقط برای درخواست‌های با وضعیت `APPROVED` در کارتابل ادمین
  دیده می‌شود.
- ⚠️ نکتهٔ معماری: این فرم **مسیر کد جدیدی نمی‌سازد.** همان
  `LoadingRequestService.markLoaded()` را صدا می‌زند که همان
  `LoadingRequestStateMachine.assertTransition(APPROVED → LOADED)` و همان تراکنش
  `markLoadedWithDelivery` (ساخت Delivery + کاهش `Order.remainingQty`) را اجرا می‌کند.
  تنها تفاوت، **مبدأ دادهٔ توزین** است (فرم ادمین به‌جای ERP). هنگام فعال شدن ERP،
  پارامتر `manual` داده نمی‌شود و همین جریان بدون تغییر کد ادامه می‌یابد.
- فیلدهای فرم: شماره توزین، تاریخ، باربری (اختیاری — از فهرست باربری‌های فعال)،
  شماره ماشین، راننده، موبایل راننده، مقدار تحویل‌شده (پیش‌فرض = مقدار اعلام‌شده،
  قابل کاهش). مقدار تحویل نمی‌تواند از مقدار اعلام‌شده بیشتر باشد.
- **هیچ فیلد مالی‌ای در فرم نیست و نباید اضافه شود** (BR-18: پورتال قیمت‌گذاری
  نمی‌کند). `basePrice` / `baseAmount` / `vatAmount` / `amountWithFactors` روی رکورد
  `Delivery` **`null`** می‌مانند — نه صفر. صفر یعنی «رایگان» و جمع‌ها را خراب می‌کند.

---

## ۴. نمایش null در جدول‌ها (رفتار عمومی، نه استثنای پایلوت)

این رفتار در خودِ `SmartTable` و `lib/format.ts` پیاده شده و به همهٔ جدول‌ها اعمال
می‌شود، نه فقط جدول تحویل:

- سلولی که مقدارش `null`/`undefined` باشد به‌جای عدد یا صفر، `—` نشان می‌دهد.
- ردیف جمع فقط مقادیر غیر‌`null` را جمع می‌زند؛ اگر همهٔ رکوردهای بازه `null` باشند،
  آن ستون جمع هم `—` نمایش می‌دهد (نه `0`).
- ستون‌های مالی جدول تحویل **حذف نشده‌اند**؛ فقط خالی نمایش داده می‌شوند و با اتصال
  ERP خودبه‌خود پر می‌شوند.
- ستون «موبایل راننده» به جدول تحویل اضافه شده است: پس از «راننده» و پیش از «محصول» —
  هم در جدول وب و هم در خروجی Excel.
  ⚠️ خروجی **PDF** عمداً تغییر نکرد: آن گزارش یک نمای فشرده برای چاپ است و از ابتدا
  ستون «راننده» هم نداشته، پس جای «پس از راننده» در آن تعریف نشده است. افزودن ستون به
  آن، تغییرِ چیدمانِ یک گزارشِ چاپیِ تأییدشده بود و خارج از دامنهٔ این فاز.

---

## ۵. چک‌لیست خروج از پایلوت (Pilot Exit)

به ترتیب اجرا شود:

1. **بایگانی داده‌های دستی — نه حذف.**
   رکوردهای `Order.source = 'MANUAL'` و `Delivery`های متصل به آن‌ها **هرگز حذف
   نمی‌شوند.** این‌ها دادهٔ واقعی دورهٔ پایلوت‌اند و مبنای صورت‌حساب/پیگیری مشتری.
   روش بایگانی: علامت‌گذاری با قرارداد Soft Delete موجود یا انتقال به جدول/اسکیمای
   آرشیو پس از تطبیق با ERP. پیش از هر اقدام، Backup دیتابیس گرفته شود.
   - یافتن آن‌ها: `SELECT * FROM "Order" WHERE source = 'MANUAL';`
     (یا از روی پیشوند `MO-` در `orderNumber`).
2. **خاموش کردن ثبت دستی:**
   ```env
   FEATURE_MANUAL_ORDER_ENTRY=false
   FEATURE_MANUAL_DELIVERY_ENTRY=false
   ```
   اثر: دکمه‌های پایلوت از UI ادمین محو می‌شوند و مسیرهای مربوطه ۴۰۳ می‌دهند.
   مسیر `mark-loaded` قبلی دست‌نخورده باقی می‌ماند.
3. **روشن کردن مالی و سفارشات:**
   ```env
   FEATURE_FINANCE_ENABLED=true
   FEATURE_ORDERS_ENABLED=true
   ```
   اثر: آیتم‌های Navbar از حالت کم‌رنگ خارج و قابل کلیک می‌شوند؛ `/finance/*` و
   `/orders/*` دقیقاً مثل امروز (همان کد تست‌شده) پاسخ می‌دهند.

   ⚠️ هر دو مرحلهٔ ۲ و ۳ را در **هر دو** محل ست کنید: `apps/api/.env` برای اجرای
   محلی و `.env` ریشه برای اجرای Docker (جدول «پرچم‌ها را کجا ست کنیم؟» در بخش ۱).
   ست کردن فقط یکی از آن‌ها، محیط دیگر را در حالت پایلوت جا می‌گذارد.
4. **فعال‌سازی ERP:**
   ```env
   ERP_ADAPTER=sql-server
   ```
   و پر کردن مقادیر `ERP_SQL_*` در `.env` طبق بخش ۱۲ PRD. کد Adapter تغییر نمی‌کند؛
   فقط Token تزریق‌شده عوض می‌شود.
5. **ری‌استارت API** (Frontend نیازی به Build مجدد ندارد).
6. **تأیید نهایی:** `GET /api/v1/feature-flags` باید خروجی زیر را بدهد:
   ```json
   {
     "FEATURE_FINANCE_ENABLED": true,
     "FEATURE_ORDERS_ENABLED": true,
     "FEATURE_MANUAL_ORDER_ENTRY": false,
     "FEATURE_MANUAL_DELIVERY_ENTRY": false
   }
   ```

---

## ۶. تغییرات دیتابیس این فاز

از طریق Prisma Migration اعمال شده‌اند (نه `db push`):

- `Order.source` — enum جدید `OrderSource` (`MANUAL` | `ERP_SYNC`)، پیش‌فرض
  `ERP_SYNC`، به‌همراه ایندکس.
- `Delivery.driverMobile` — `String?`.
- چهار ستون مالی `Delivery` (`basePrice`, `baseAmount`, `vatAmount`,
  `amountWithFactors`) به Nullable تغییر کردند. `deductions` همچنان `NOT NULL` با
  پیش‌فرض صفر است (کسورات نداشتن یعنی صفر، نه نامعلوم).

⚠️ این تغییرات اسکیمایی **برگشت‌پذیر با پرچم نیستند** و لازم هم نیست باشند: افزودن
ستون Nullable و شل‌کردن قید NOT NULL، رفتار موجود را نمی‌شکند و پس از پایان پایلوت
هم برای دادهٔ ERP مفید می‌ماند (ERP ممکن است موبایل راننده نفرستد).

---

## ۷. پاک‌سازی دادهٔ ماک (Mock Seed Data)

گیت‌کردن Seed با `SEED_MOCK_DATA` فقط جلوی **درج مجدد** داده ماک را می‌گیرد؛ رکوردهایی
که قبلاً در دیتابیس Seed شده‌اند با آن حذف نمی‌شوند. برای دیتابیس‌های موجود (از جمله
دیتابیس پایلوت) اسکریپت جداگانهٔ زیر اضافه شده است:

`apps/api/prisma/cleanup-mock-data.ts`

این اسکریپت **فقط** رکوردهایی را حذف می‌کند که بتوان با قطعیت اثبات کرد خروجی
`seed.ts` هستند (پیشوندهای شناسهٔ قطعی `seed_ord_` / `seed_lr_` / `seed_del_` /
`seed_fin_`، هویت فرمولی مشتری Seed، و مقادیر رشته‌ای دقیق Seed). هر رکوردی که
اثبات‌پذیر نباشد **دست‌نخورده** می‌ماند. `Product`، `Carrier`، ادمین اصلی `admin`،
`AuditLog`، `ErpSyncLog` و `Attachment` هرگز لمس نمی‌شوند.

### اجرای Dry-Run (پیش‌فرض — هیچ نوشتنی در دیتابیس)

```bash
pnpm db:cleanup-mock
```

خروجی شامل دیتابیس هدف (بدون رمز)، تعداد و نمونهٔ رکوردهای هر مدل، قاعدهٔ تشخیص هر
مدل، و فهرست «نگه‌داشته‌شده‌ها» (مشتریان ماکی که وابستهٔ اثبات‌ناپذیر دارند و به همین
دلیل حذف نمی‌شوند) است. **هیچ تغییری در دیتابیس ایجاد نمی‌شود.**

### اجرای واقعی (حذف تأییدشده)

ابتدا Backup بگیرید:

```bash
pg_dump "$DATABASE_URL" > backup-before-cleanup.sql
```

سپس:

```bash
CONFIRM_DELETE_MOCK_DATA=true pnpm db:cleanup-mock
```

روی PowerShell:

```powershell
$env:CONFIRM_DELETE_MOCK_DATA="true"; pnpm db:cleanup-mock
```

⚠️ فقط رشتهٔ دقیق `true` مجاز است (همان قرارداد `SEED_MOCK_DATA`). حذف در یک
Transaction و به ترتیب Child→Parent انجام می‌شود و دقیقاً همان شناسه‌هایی حذف
می‌شوند که در Dry-Run گزارش شده‌اند. از `prisma migrate reset` استفاده **نمی‌شود**.
